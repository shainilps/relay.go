app:
  network: # main/test

db:
  url: #sqlite db path

key:
  wif_path: #wif path use .txt file
  mnemonic_path:
  password: #password in case of new key

# default_url: taal
arc:
  token:
  # provider: taal (future plan)

# default_url: whatsonchain
explorer:
  token:
  # provider: whatsonchain (future plan)

# try to update this manually by acc to boradcaster policy
# example:
# curl --location 'https://arc.taal.com/v1/policy' \
#      --header 'Authorization: <API_KEY> | jq
fee:
  bytes_per_sat: 1000
